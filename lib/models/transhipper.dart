class Envios {
  String title = '';
  String url = '';
  String weightContent = '';

  Envios(title, url, weightContent) {
    title = this.title;
    url = this.url;
    weightContent = this.weightContent;
  }
}
